#!/bin/sh

# Exit on error
set -e

echo "🔍 Running pre-commit checks..."

# Get list of staged files (save for re-staging after formatters run)
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACMR)
STAGED_FILES_LIST=$(echo "$STAGED_FILES" | tr '\n' ' ')

# Check if any TypeScript/JavaScript files are staged
HAS_TS_FILES=$(echo "$STAGED_FILES" | grep -E '\.(ts|tsx|js|jsx)$' || true)

# Check if any Python files are staged
HAS_PY_FILES=$(echo "$STAGED_FILES" | grep -E '\.py$' || true)

# Check if any Rust files are staged
HAS_RS_FILES=$(echo "$STAGED_FILES" | grep -E '\.rs$' || true)

# Check if any package.json files are staged
HAS_PKG_FILES=$(echo "$STAGED_FILES" | grep 'package\.json$' || true)

# 1. Biome: Format + Lint TypeScript/JavaScript/JSON (staged files only, with fixes)
echo "📝 Biome: Formatting and linting staged TS/JS/JSON files..."
bunx @biomejs/biome check --write --staged --files-ignore-unknown=true --no-errors-on-unmatched

# 2. TypeScript: Type checking (if TS/JS files are staged)
if [ -n "$HAS_TS_FILES" ]; then
  echo "🔷 TypeScript: Running type checker..."
  bun run typecheck || {
    echo "❌ TypeScript type errors found. Please fix them before committing."
    exit 1
  }
fi

# 3. Ruff: Format Python (all Python dirs, if they exist)
if [ -d "apps/vision-service" ] || [ -d "packages/research" ]; then
  echo "🐍 Ruff: Formatting Python files..."
  ruff format apps/vision-service packages/research 2>/dev/null || true

  # 4. Ruff: Lint Python with auto-fix
  echo "🐍 Ruff: Linting Python files..."
  ruff check --fix apps/vision-service packages/research 2>/dev/null || true

  # 5. Mypy: Type checking Python (if Python files are staged)
  if [ -n "$HAS_PY_FILES" ]; then
    echo "🐍 Mypy: Running type checker..."
    # Run mypy on each Python project that has staged files
    for dir in packages/research apps/vision-service; do
      if [ -d "$dir" ] && echo "$STAGED_FILES" | grep -q "^$dir/"; then
        echo "  → Checking $dir..."
        (cd "$dir" && uv run mypy . 2>/dev/null) || {
          echo "❌ Python type errors found in $dir. Please fix them before committing."
          exit 1
        }
      fi
    done
  fi
fi

# 6. Cargo: Format Rust (if Rust files exist)
if [ -d "apps/execution-engine" ]; then
  echo "🦀 Cargo: Formatting Rust files..."
  cargo fmt --all 2>/dev/null || true

  # 7. Clippy: Lint Rust (includes type checking)
  # Use -D clippy::correctness for actual bugs, allow style/complexity lints
  echo "🦀 Clippy: Checking Rust files..."
  cargo clippy --all-targets --all-features -- \
    -D clippy::correctness \
    -W clippy::all \
    2>/dev/null || {
    echo "❌ Rust errors found. Please fix them before committing."
    exit 1
  }
fi

# 8. Dependency validation (if package.json files are staged)
if [ -n "$HAS_PKG_FILES" ]; then
  echo "📦 Validating package dependencies..."
  bun run lint:deps || {
    echo "❌ Dependency validation failed. Please fix circular or invalid dependencies."
    exit 1
  }
fi

# Re-stage only the originally staged files (in case formatters modified them)
if [ -n "$STAGED_FILES_LIST" ]; then
  echo "📦 Re-staging fixed files..."
  echo "$STAGED_FILES_LIST" | xargs git add --
fi

echo "✅ Pre-commit checks complete!"
