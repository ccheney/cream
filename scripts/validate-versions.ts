#!/usr/bin/env bun
/**
 * Version Validation Script (Wrapper)
 *
 * This file forwards to the modular implementation in ./validate-versions/index.ts.
 * Run with `bun run validate` or `bun scripts/validate-versions.ts`.
 */

import "./validate-versions/index.js";
