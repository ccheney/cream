{
  "name": "@cream/test-fixtures",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    },
    "./snapshots/*": "./snapshots/*",
    "./decisions/*": "./decisions/*",
    "./memory/*": "./memory/*"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "typecheck": "tsc --noEmit",
    "test": "CREAM_ENV=BACKTEST bun test",
    "lint": "biome check src/",
    "format": "biome format --write src/",
    "clean": "rm -rf dist coverage"
  },
  "dependencies": {
    "@cream/domain": "workspace:*",
    "@cream/mocks": "workspace:*",
    "deepmerge-ts": "^7.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
