{
	"name": "@cream/alpaca-stream-proxy",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "set -a; [ -f ../../.env ] && . ../../.env; set +a; cargo run",
		"build": "cargo build --release",
		"test": "cargo test",
		"format": "cargo fmt",
		"lint": "cargo clippy --fix --allow-dirty --allow-staged --all-targets --all-features -- -D warnings",
		"clean": "cargo clean"
	}
}
