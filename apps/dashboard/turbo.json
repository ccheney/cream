{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"],
      "env": ["CREAM_ENV"],
      "passThroughEnv": ["CREAM_ENV"]
    }
  }
}
