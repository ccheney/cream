{
  "name": "@cream/execution-engine",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "set -a && source ../../.env 2>/dev/null && set +a && cargo run",
    "build": "cargo build --release",
    "test": "cargo test",
    "format": "cargo fmt",
    "lint": "cargo clippy --all-targets --all-features -- -D warnings",
    "clean": "cargo clean"
  }
}
